<svg class="slds-button__icon
    {{size ? 'slds-button__icon_${size}' : ''}}
    {{cssClass ? cssClass : ""}}" 
    aria-hidden="true"
    {{focusable ? "focusable='true'" : ""}}
    >
        <use xlink:href="/assets/icons/{{iconType}}-sprite/svg/symbols.svg#{{iconName}}" style="pointer-events: none;"></use>
</svg>